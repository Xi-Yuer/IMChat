import { FC, memo } from 'react'
import { IMessagePanelProps } from './DocxMessage'

const VoiceMessage: FC<IMessagePanelProps> = memo(() => {
  return <div>VoiceMessage</div>
})

export default VoiceMessage
